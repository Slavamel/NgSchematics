{
  "name": "@schem/dhl-service",
  "version": "0.0.1",
  "description": "Creating services in dhl way",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "test": "npm run build && jasmine src/**/*_spec.js"
  },
  "keywords": [
    "schematics"
  ],
  "author": "",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^9.1.6",
    "@angular-devkit/schematics": "^9.1.6",
    "typescript": "~3.8.2"
  },
  "devDependencies": {
    "@angular/cli": "^9.1.6",
    "@types/jasmine": "~3.5.0",
    "@types/node": "^12.11.1",
    "jasmine": "^3.5.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
